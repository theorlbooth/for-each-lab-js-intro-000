function interativeLog(array) {
  array.forEach()
    console.log(`${index}:${element}`)
}

function iterate(callback) {
an_array = ["milk", "cat", "pop"]
    an_array.forEach(callback)
    return an_array
}

